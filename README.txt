The python script creates a tar ball of the files present in the directory specified and backs them up to the google drive.

The credentials are stored with the program so that this can be done automatically.

Also the script performs the clean up after the transfer completes.